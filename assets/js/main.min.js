var multiItemSlider = (function () {
    return function (selector, config) {
        var
            mainBlock = document.querySelector(selector),
            sliderWrapper = mainBlock.querySelector('.ddsWrapper'),
            sliderItems = mainBlock.querySelectorAll('.ddsItem'),
            sliderControls = mainBlock.querySelectorAll('.ddsSliderControl'),
            sliderControlLeft = mainBlock.querySelector('.ddsSliderControl_left'),
            sliderControlRight = mainBlock.querySelector('.ddsSliderControl_right'),
            wrapperWdth = parseFloat(getComputedStyle(sliderWrapper).width),
            itemWidth = parseFloat(getComputedStyle(sliderItems[0]).width),
            positionLeftItem = 0,
            transform = 0,
            step = itemWidth / wrapperWdth * 100,
            items = [];
        sliderItems.forEach(function (item, index) {
            items.push({item: item, position: index, transform: 0});
        });

        var position = {
            getMin: 0,
            getMax: items.length - 1,
        }

        var transformItem = function (direction) {
            if (direction === 'right') {
                if ((positionLeftItem + wrapperWdth / itemWidth - 1) >= position.getMax) {
                    return;
                }
                if (!sliderControlLeft.classList.contains('ddsSliderControl_show')) {
                    sliderControlLeft.classList.add('ddsSliderControl_show');
                }
                if (sliderControlRight.classList.contains('ddsSliderControl_show') && (positionLeftItem + wrapperWdth / itemWidth) >= position.getMax) {
                    sliderControlRight.classList.remove('ddsSliderControl_show');
                }
                positionLeftItem++;
                transform -= step;
            }
            if (direction === 'left') {
                if (positionLeftItem <= position.getMin) {
                    return;
                }
                if (!sliderControlRight.classList.contains('ddsSliderControl_show')) {
                    sliderControlRight.classList.add('ddsSliderControl_show');
                }
                if (sliderControlLeft.classList.contains('ddsSliderControl_show') && positionLeftItem - 1 <= position.getMin) {
                    sliderControlLeft.classList.remove('ddsSliderControl_show');
                }
                positionLeftItem--;
                transform += step;
            }
            sliderWrapper.style.transform = 'translateX(' + transform + '%)';
        }
        var controlClick = function (e) {
            if (e.target.classList.contains('ddsSliderControl')) {
                e.preventDefault();
                var direction = e.target.classList.contains('ddsSliderControl_right') ? 'right' : 'left';
                transformItem(direction);
            }
        };

        var setUpListeners = function () {
            sliderControls.forEach(function (item) {
                item.addEventListener('click', controlClick);
            });
        }
        setUpListeners();

        return {
            right: function () {
                transformItem('right');
            },
            left: function () {
                transformItem('left');
            }
        }

    }
}());
var slider = multiItemSlider('.slider')


function i(c) {
    return document.getElementById(c);
}

var url = '/assets/ajax/mailSend.php';
i('sendThisForm').addEventListener('submit', postData);

function postData(event) {
    event.preventDefault();
    const formData = new URLSearchParams(new FormData(i('sendThisForm'))).toString();
    fetch(url, {
        method: 'POST',
        body: formData,
    }).then(response => response.json())
        .then((data) => alert(data));
    i('sendThisForm').reset();
}

myButton = i("myBtn");

window.onscroll = function () {
    scrollFunction()
};

function scrollFunction() {
    if (document.body.scrollTop > 20 || document.documentElement.scrollTop > 20) {
        myButton.style.display = "block";
    } else {
        myButton.style.display = "none";
    }
}

function topFunction() {
    document.body.scrollTop = 0;
    document.documentElement.scrollTop = 0;
}

myButton.addEventListener('click', topFunction)

function youtubeParser(url) {
    var regExp = /^.*((youtu.be\/)|(v\/)|(\/u\/\w\/)|(embed\/)|(watch\?))\??v?=?([^#&?]*).*/;
    var match = url.match(regExp);
    return (match && match[7].length == 11) ? match[7] : false;
}

function youtubeInsert() {
    var src = youtubeParser(i('youtubeVideo').value);
    if (src == false) {
        src = i('youtubeVideo').value
    }
    if (youtubeParser('https://www.youtube.com/embed/' + src) != false) {
        i('myFrame').setAttribute('src', 'https://www.youtube.com/embed/' + src);
    } else {
        alert('Ссылка неверна')
    }
}

youButton = i("youButton");
youButton.addEventListener('click', youtubeInsert);

function putJsonInTextBlock() {
    if (i('jsonInput').value > 100) {
        fetch('https://jsonplaceholder.typicode.com/posts/1')
            .then((response) => response.json())
            .then((json) => i('text').innerHTML = '<p><b>Сайт предлогает только 100 вариантов, вы ввели число больше чем 100, поэтому по умолчанию отправляем 1:</b></p><p>POST ID = ' + json.id + '</p><p>TITLE = ' + json.title + '</p><p>BODY = ' + json.body + '</p>')

    } else {
        if (i('jsonInput').value.length > 0) {
            fetch('https://jsonplaceholder.typicode.com/posts/' + i('jsonInput').value)
                .then((response) => response.json())
                .then((json) => i('text').innerHTML = '<p>POST ID = ' + json.id + '</p><p>TITLE = ' + json.title + '</p><p>BODY = ' + json.body + '</p>')
        } else {
            fetch('https://jsonplaceholder.typicode.com/posts/1')
                .then((response) => response.json())
                .then((json) => i('text').innerHTML = '<p><b>Вы не ввели число, поэтому берем 1 по умолчанию:</b></p><p>POST ID = ' + json.id + '</p><p>TITLE = ' + json.title + '</p><p>BODY = ' + json.body + '</p>')
        }

    }

}

jsonButton = i("jsonButton");
jsonButton.addEventListener('click', putJsonInTextBlock);
var allPageButtons = document.querySelectorAll('button');
allPageButtons.forEach(element => element.addEventListener('click', function () {
    var cssType = this.getAttribute("data-css"), cssValue = this.getAttribute("data-value");

    if (cssType && cssValue) {
        switch (cssType) {
            case 'color':
                i('textArea').style.color = cssValue;
                break;
            case 'fontSize':
                i('textArea').style.fontSize = cssValue;
                break;
            case 'margin':
                i('textArea').style.margin = cssValue;
                i('textArea').style.maxWidth = '500px';
                break;
            case 'textAlign':
                i('textArea').style.textAlign = cssValue;
                break;
            case 'backgroundColor':
                i('buttons').style.backgroundColor = cssValue;
                break;
            default:
                alert("Нет таких значений");
        }
    }
    if (!cssValue) {
        switch (cssType) {
            case 'remove':
                i('textArea').remove()
                break;
            case 'insert':
                if(i('textArea')){
                    alert('Нельзя вставить более одного блока с текстом')
                } else {
                    i('buttons').insertAdjacentHTML('beforeend', '<div id="textArea">\n' +
                        '        <p>Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry\'s standard dummy text ever since the 1500s, when an unknown printer took a galley of type and scrambled it to make a type specimen book. It has survived not only five centuries, but also the leap into electronic typesetting, remaining essentially unchanged. It was popularised in the 1960s with the release of Letraset sheets containing Lorem Ipsum passages, and more recently with desktop publishing software like Aldus PageMaker including versions of Lorem Ipsum.</p>\n' +
                        '        <p>Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry\'s standard dummy text ever since the 1500s, when an unknown printer took a galley of type and scrambled it to make a type specimen book. It has survived not only five centuries, but also the leap into electronic typesetting, remaining essentially unchanged. It was popularised in the 1960s with the release of Letraset sheets containing Lorem Ipsum passages, and more recently with desktop publishing software like Aldus PageMaker including versions of Lorem Ipsum.</p>\n' +
                        '    </div>')
                }

                break;
            case 'cancel':
                i('buttons').style.backgroundColor = 'white';
                i('textArea').remove();
                i('buttons').insertAdjacentHTML('beforeend', '<div id="textArea">\n' +
                    '        <p>Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry\'s standard dummy text ever since the 1500s, when an unknown printer took a galley of type and scrambled it to make a type specimen book. It has survived not only five centuries, but also the leap into electronic typesetting, remaining essentially unchanged. It was popularised in the 1960s with the release of Letraset sheets containing Lorem Ipsum passages, and more recently with desktop publishing software like Aldus PageMaker including versions of Lorem Ipsum.</p>\n' +
                    '        <p>Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry\'s standard dummy text ever since the 1500s, when an unknown printer took a galley of type and scrambled it to make a type specimen book. It has survived not only five centuries, but also the leap into electronic typesetting, remaining essentially unchanged. It was popularised in the 1960s with the release of Letraset sheets containing Lorem Ipsum passages, and more recently with desktop publishing software like Aldus PageMaker including versions of Lorem Ipsum.</p>\n' +
                    '    </div>')
                break;

            default:
                alert("Нет таких значений");
        }
    }

}, false))


/*const myAmimation = document.querySelectorAll('.smart-scroll');

if (myAmimation.length > 0) {
    window.addEventListener('scroll', animatedScroll)
    function animatedScroll() {
        for (let index = 0; index < myAmimation.length; index++) {
            const myAnimatedItem = myAmimation[index];
            const myAnimatedItemHeight = myAnimatedItem.offsetHeight;
            const myAnimatedItemOffset = offset(myAnimatedItem).top;
            const myAnimationStart = 6;

            let animationPoint = window.innerHeight - myAnimatedItemHeight / myAnimationStart;

            if (myAnimatedItemHeight > window.innerHeight) {
                animationPoint = window.innerHeight - window.innerHeight / myAnimationStart;
            }

            if ((pageYOffset > myAnimatedItemOffset - animationPoint) && pageYOffset < (myAnimatedItemOffset + myAnimatedItemHeight)) {
                myAnimatedItem.classList.add('visible');
            }
        }
    }
    function offset(el) {
        const rect = el.getBoundingClientRect(),
            scrollLeft = window.pageXOffset || document.documentElement.srcrollLeft,
            scrollTop = window.pageYOffset || document.documentElement.scrolltTop;
        return {top: rect.top + scrollTop, left: rect.left + scrollLeft}
    }
}
animatedScroll();*/

